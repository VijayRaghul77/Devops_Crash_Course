apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "private-nginx.fullname" . }}-conf
  labels:
    {{- include "private-nginx.labels" . | nindent 4 }}
data:
  nginx.conf: |
{{ .Values.nginxConf | indent 4 }}
